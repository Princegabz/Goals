package com.example.goals

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class GoalDetailActivity : AppCompatActivity() {

    // Initialize your database helper class here

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_goal_detail)

        // Handle saving a new goal or editing an existing one
        // Update the database with the goal information

        // Handle marking the goal as completed
    }
}
